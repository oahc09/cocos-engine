# 2026-04-27 工作日志

## Phase D2: 场景切换释放策略收口 ✅
- 修改 `Director::destroyOldScene()` 实现场景切换时自动释放策略
- 实现：walk 旧场景所有节点，通过 `Component::getAssetProperties()` 收集内置组件资产引用，通过 `ScriptBridge::collectAssetRefs()` 收集脚本组件资产引用
- 收集所有唯一 UUID 后统一 `decRef`，场景销毁后调用 `ReleaseManager::autoRelease()`
- persist root nodes 在 `handlePersistRootNodes()` 中已迁移到新场景，不会被误释放
- 新增 include: `ReleaseManager.h`, `ScriptBridge.h`, `ScriptComponent.h`, `unordered_set.h`

## Phase D3: AssetManager 引用计数闭环 ✅
- `cacheAsset()`: registerAsset + addRef（缓存持有一个逻辑引用）
- `removeCachedAsset()`: decRef 释放缓存的逻辑引用（不再调 unregisterAsset）
- `clearCache()`: 逐项 decRef 后再清除缓存
- 语义对齐：缓存入口/出口与 ReleaseManager addRef/decRef 完全配对

## 编译验证
- Debug + Release 均零错误通过
- Director.cpp 和 AssetManager.cpp 均重新编译

## Phase D 状态
- D1 权威源 ✅ (之前完成)
- D2 释放策略收口 ✅
- D3 AssetManager 闭环 ✅
- **Phase D 全部完成**
