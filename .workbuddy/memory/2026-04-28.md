# 2026-04-28 工作日志

## Phase E Builtin Component JSB 绑定完成

### C2065 编译错误修复
- **根因**: `register_all_camera_component` 声明在 `namespace cc` 中，但 `jsb_module_register.cpp` 不在任何命名空间
- **修复**: 移除 namespace cc 包装，改为全局命名空间声明（与其他 register_all_* 函数一致）

### MeshRendererComponent JSB 绑定 (Phase E4)
- 新建 `jsb_mesh_renderer_manual.h/.cpp`
- 绑定属性: shadowCastingMode, receiveShadow
- 绑定方法: setMesh, getMesh, setMaterial, getMaterial, getRenderModel
- **踩坑**: `sevalue_to_native<T*>` 对未注册 `is_jsb_object` 的类型不可用，改用 `static_cast<T*>(obj->getPrivateData())`
- **踩坑**: `native_ptr_to_seval<Mesh>` 需要 Mesh 完整类型定义，需要 `#include "3d/assets/Mesh.h"`

### LightComponent JSB 绑定 (Phase E5)
- 新建 `jsb_light_component_manual.h/.cpp`
- 5个灯光类全部绑定: DirectionalLight, SphereLight, SpotLight, PointLight, RangedDirectionalLight
- 各自的属性: color(Vec3), illuminance, range, size, luminance, spotAngle, shadow系列
- **踩坑**: 自定义 Vec3_to_seval 与 jsb_conversions_spec.h 中已有函数冲突，改为使用已有版本

### 编译结果
- Debug + Release 全量编译通过，零错误
