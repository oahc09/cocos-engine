# 2026-05-02 D3D12 后端完整性审查 + 修复

## 审查结果
- 对 gfx-d3d12/ 全部 16 个 GFX 类做了逐方法对比基类纯虚接口
- **所有纯虚方法 100% 覆盖**，类结构完整
- 报告保存在 `AI/d3d12-backend-audit-report.md`

## 发现并修复的问题 (全部已修复, Debug 编译通过)
1. **P0 ✅**: `draw()` 添加 indirect draw — 检查 IA.indirectBuffer, 使用 ExecuteIndirect
2. **P0 ✅**: `dispatch()` 添加 indirect dispatch — 检查 DispatchInfo.indirectBuffer
3. **P1 ✅**: `setDepthBound()` — 使用 ID3D12GraphicsCommandList1::OMSetDepthBounds
4. **P1 ✅**: `customCommand()` — 传入 ID3D12GraphicsCommandList* 指针
5. **P1 ✅**: `getMaxSampleCount()` — CheckFeatureSupport 多重采样查询
6. **P3 ✅**: 清理 ~144 处诊断日志 — 涉及 6 个文件

## 新增 Device 组件
- Command Signatures: drawIndirectSig, drawIndexedIndirectSig, dispatchIndirectSig
- getter: getDrawIndirectSignature(), getDrawIndexedIndirectSignature(), getDispatchIndirectSignature()

## 无需修复的已知限制
- setLineWidth: D3D12 不支持 (正确处理)
- setStencilWriteMask: 在 PSO 中处理 (正确处理)
- ETC2/ASTC: 已有 RGBA8 回退
- states/ 子目录: barrier 逻辑内联在 CommandBuffer 中

## D3D12 半透明渲染逻辑排查 (下午)
- 完整分析报告: `AI/d3d12-alpha-blend-analysis.md`
- **BlendFactor 映射**: 15个枚举全部映射，CONSTANT_ALPHA/CONSTANT_COLOR 因 D3D12 API 限制映射到同一值（不影响当前内置 Effect）
- **BlendState → PSO 填充**: 结构正确，颜色/Alpha通道独立配置 ✅
- **❌ H1 Bug: blendColor 未自动传递到 OMSetBlendFactor** — `bindPipelineState` 中缺失，WebGL 后端有但 D3D12 没有
- **⚠️ M1: SampleDesc.Count 硬编码为 1** — MSAA 管线未连通，alpha-to-coverage 无效
- **标准半透明混合**: src_alpha / one_minus_src_alpha 数据流验证正确 ✅
- 当前内置 Effect 不使用 CONSTANT_COLOR/CONSTANT_ALPHA，H1 暂不触发
