# 2026-05-12 工作日志

## D3D12 离屏渲染 RT 绑定问题分析 (RenderDoc capture)

### 问题
- Capture: `C:\Users\caosh\AppData\Local\Temp\RenderDoc\test-cases_2026.05.11_16.20.37_frame607.rdc`
- Colour Pass #1 (EID 559-581) 的 3D 模型绘制(12636 indices)直接绘制到了 swapchain back buffer (ResourceId::449, 800×600)
- 本应绘制到离屏纹理 (ResourceId::2118, 512×512, 10 mips) 但该纹理仅作为 PS SRV slot 0 被采样

### 结论
- **不是 D3D12 GFX 后端 bug** — beginRenderPass/getRTVHandle 逻辑正确
- **问题在 NativePipeline 的 FrameGraph 资源分配层**
  - `FrameGraphDispatcher.cpp:338-345` 的 RenderSwapchain 分支将 swapchain back buffer 作为 color attachment
  - Colour Pass #1 的 output 资源被错误注册为 RenderSwapchain(带 swapchain 指针) 而非 ManagedTexture
- 修复方向: 检查 FrameGraph pass 定义中 RasterView 的 output color attachment 绑定是否正确指向离屏纹理
