# 2026-04-30 工作日志

## D3D12 材质支持 — 真实 Shader PSO 创建成功

### 问题链
1. SM 5.0 不支持 `register(bN, spaceS)` → 升级 SM 5.1
2. SPIRV-Cross `add_hlsl_resource_binding` 的 stage 字段默认 `ExecutionModelMax`，导致 `remap_hlsl_resource_binding` 查找键不匹配 → 添加 `toSPIRVExecutionModel()` 映射
3. register_space 先尝试全 force=0（不匹配 PipelineLayout），后改为 set=N（匹配）
4. **根因**: D3DCompile 入口点用 `vert_main`/`frag_main`（无 SV_Position 语义的内部函数），应改为 `main`（SPIRV-Cross 真正的入口函数）

### 修复文件
- `D3D12Shader.cpp`: 
  - SM 5.1, HLSL profile vs_5_1/ps_5_1
  - 新增 `toSPIRVExecutionModel()` 函数
  - `addBinding` 中设置 `hlslBinding.stage` 和 `register_space = set`
  - 入口点从 `vert_main`/`frag_main` 改为 `"main"`
- `D3D12PipelineLayout.cpp`: 添加绑定诊断日志
- `D3D12PipelineState.cpp`: 三层 PSO 重试逻辑 + 详细诊断

### 结果
- 104/104 着色器 GLSL→SPIR-V→HLSL→DXBC 编译成功
- 3 个 PSO primary path 创建成功（之前全是 fallback）
- WebGPUDemo 稳定运行 25 秒，零错误

## D3D12 Gate 3 修复 — InputLayout/CBV/IA 偏移

### 修复链
1. InputLayout 语义名 static vector→use-after-free → 改为 `_impl->semanticNames` 每 PSO 实例存储
2. SPIRV-Cross 将所有 GLSL 顶点输入映射为 TEXCOORD0/1/2...，InputLayout 必须匹配（不是 POSITION/NORMAL）
3. DescriptorSet CBV 小 buffer 跳过创建但 offset 递增→堆偏移错乱 → 写入 null 描述符保持偏移一致
4. InputAssembler resourceOffset 双重计算 → 移除多余的 `+ getD3D12ResourceOffset()`

### 修复文件
- `D3D12PipelineState.h/.cpp`: _impl->semanticNames 存储, TEXCOORD 语义映射
- `D3D12DescriptorSet.cpp`: 小 buffer null 描述符
- `D3D12InputAssembler.cpp`: VB/IB view 偏移修复

### 结果
- 6/6 PSO primary path 创建成功，零 fallback
- 20 个 draw call 正常执行
- Debug/Release 编译通过

## 文档更新
- 更新 `AI/D3D12-Material-Support-Plan.md`：Phase 1-8 全部标记 ✅，Phase 9 标记 🔄 进行中
- 新增"已解决的关键问题"表格（10 项）、"当前运行状态"和"已知待解决项"章节

## 编译验证 (11:15)
- **引擎库**: Debug + Release 均编译通过 (cocos_engine.lib)
- **WebGPUDemo**: Release 编译+链接通过 (WebGPUDemo.exe)
- **运行时验证**: 稳定运行 25 秒
  - 50 个 Shader 全部初始化成功 (2D sprite + builtin-standard + skybox + particles 等)
  - 10 个 draw call 正常执行 (idx=6 sprite 四边形, pso=YES)
  - DeviceRemovedReason 全程 0x00000000 (OK)
  - 零错误、零崩溃、零 fallback

## Code Review 修复 (11:58)

### 已修复
1. **Critical**: CommandBuffer::copyBuffersToTexture 上传资源生命周期 bug
   - uploadResource ComPtr 在 for 循环内是局部变量，循环结束后释放
   - 修复: 新增 `_impl->pendingUploadResources` vector，push_back(move) 保持存活
   - 在 begin() 中 clear()（此时 GPU 已完成上一帧）
2. **Minor**: toAddressMode lambda 重复定义 → 提取为匿名命名空间静态函数

### 回退（未修复）
- **Important**: DescriptorSet null binding 描述符槽未写入
  - 尝试用 `CreateShaderResourceView(nullptr, nullptr, handle)` 写入 null descriptor
  - **在 AMD 驱动上导致 DEVICE_HUNG**（0x887a0001 / DXGI_ERROR_DEVICE_HUNG）
  - 回退为原始行为：null binding 时不写入描述符，offset 仍递增
  - 后续需要用正确的 null descriptor 方式实现（需提供有效 desc 结构体而非 nullptr）

### 验证结果
- Debug + Release 编译通过
- WebGPUDemo 稳定运行 25 秒：100 个 shader、10 个 draw call、零错误

## D3D12 黑屏排查 — PipelineBarrier + 资源状态追踪 (13:00)

### 问题
WebGPUDemo 稳定运行但渲染黑屏，D3D12 Debug Layer 报大量资源屏障错误。

### 已完成
1. **pipelineBarrier 完整实现** (原为空函数)
   - `accessFlagsToD3D12State` lambda: AccessFlagBit → D3D12_RESOURCE_STATES 完整映射
   - 纹理屏障: 使用追踪的 `_currentState` 作为 StateBefore，转换后更新
   - 缓冲区屏障: 写入后读取时插入全局 UAV 屏障
   - 跳过 swapchain 纹理（由 beginRenderPass/endRenderPass 管理）

2. **D3D12Texture 资源状态追踪**
   - 新增 `getCurrentState()`/`setCurrentState()` 方法
   - `doInit(TextureInfo)`: 根据 usage 设置初始状态 (COLOR_ATTACHMENT→RENDER_TARGET, DEPTH_STENCIL→DEPTH_WRITE, SAMPLED→PIXEL_SHADER_RESOURCE)
   - `doInit(TextureViewInfo)`: 继承源纹理的 `_currentState`
   - `doInit(SwapchainTextureInfo)`: 深度→DEPTH_WRITE, 颜色→PRESENT

3. **beginRenderPass 重构**: 颜色/深度附件转换到正确状态 + 批量屏障提交
4. **endRenderPass 重构**: swapchain→PRESENT, 颜色→PIXEL_SHADER_RESOURCE, 深度→DEPTH_READ
5. **copyBuffersToTexture 状态追踪**: 使用追踪状态 + 拷贝后恢复

### 结果
- D3D12 Debug Layer severity=1 错误清零 ✅
- 程序稳定运行 1200+ 帧无崩溃 ✅
- **黑屏未解决** ❌ — 截屏显示白色/灰色(Win32窗口背景色)，D3D12渲染内容未显示到窗口

## 文档同步 (13:28)
- 更新 `AI/D3D12-Material-Support-Plan.md`:
  - 已解决关键问题表新增 #11-#14 (pipelineBarrier/状态追踪/begin-endRenderPass/copyBuffersToTexture)
  - 当前运行状态新增：pipelineBarrier完整实现、资源状态追踪、Debug Layer错误清零、1200+帧稳定、黑屏未解决
  - 已知待解决项重组为三个分类：黑屏排查(高优)、性能优化(低优)、功能待实现
  - Phase 9 拆分为 9.1 内置场景测试 + 9.2 资源状态追踪(✅) + 9.3 性能与稳定性 + 9.4 错误处理
  - 文件修改总览：D3D12CommandBuffer 和 D3D12Texture Phase 列更新为 5+9 和 8+9

### 当前任务状态
| # | 任务 | 状态 |
|---|------|------|
| 35 | 排查 pipelineBarrier no-op | ✅ 已实现完整 pipelineBarrier |
| 36 | 排查顶点数据绑定 (VB/IB) | ✅ 不需要（readback确认渲染正确） |
| 37 | 排查描述符集数据 (cbuffer/矩阵) | ✅ 不需要（readback确认渲染正确） |
| 38 | 排查 Cocos 渲染管线调用流程 | ✅ 已完成 |

### 黑屏根因与修复
- **根因**: FLIP_DISCARD + syncInterval=1 + Queue::submit同步fence wait 导致窗口显示全黑
- **具体表现**: D3D12 API readback确认back buffer有正确渲染内容，但PrintWindow/BitBlt截屏为全黑
- **修复**: Present()使用syncInterval=0（立即呈现），因为Queue::submit已经是同步的
- **验证**: syncInterval=0后截屏显示R=235 G=235 B=234（splash screen灰色），确认渲染内容正确显示到窗口
- **新增诊断**: Device::present()中添加back buffer像素readback诊断（Frame 1-5, 60, 300）
- **新增方法**: Swapchain::getCurrentBackBufferIndex() 用于诊断日志
