# 2026-05-22

## RenderDoc MCP 修复 (前次会话)
- renderdoc-mcp 因 HTTP_PROXY 环境变量导致 httpx 端口解析崩溃 (Invalid port: ':1]')
- 修复: ~/.claude.json renderdoc 配置注入 env 清除代理变量
- 15 个工具可用，连接正常

## Shadow Map Pass 分析 (EID 1416-1496)
- Capture: test-cases_2026.05.21_15.21.08_frame2200.rdc (D3D12)
- 帧结构: 4 个 Colour Pass, 31 draw calls, 9 clears, 1 present
- **Pass #1** (EID 1909 marker, 7 children): Shadow Map 生成 pass
  - RT: ResourceId::2826 (2048×2048 R32_FLOAT) — shadow depth 纹理
  - DS: ResourceId::2827 (2048×2048 D32) — shadow depth buffer
  - 5 个 draw call (各 600 索引 = 立方体 mesh)
  - VS: 用 cc_matWorld (CB2) + cc_matLightViewProj (CB0[4-7]) 变换
  - PS: 仅输出 depth = v_clip_depth.x / v_clip_depth.y (线性化深度)
  - **问题**: PS 无 SRV 纹理绑定，无 sampler，无常量缓冲区 — 这是正确的 shadow depth-only pass
- **Pass #2** (EID 1910 marker, 17 children): 主渲染 pass
  - RT: ResourceId::448 (swapchain back buffer)
  - DS: ResourceId::1044
  - PS 绑定 shadow map 作为 SRV: ResourceId::2826 (2048×2048 R32_FLOAT)
  - PS 绑定 CCCamera (592B), CCShadow (256B), Constants (96B)
  - 6 个 draw call (5×600 + 1×36 索引)

### 深入 Shader 分析
- **Pass #1 有两种 shader 变体**:
  - EID 1436-1481 (前4个draw): VS#2831 + PS#2832 (简单 depth-only, `output = z/w`)
  - EID 1496 (最后draw): VS#2834 + PS#2835 (复杂 shadow depth, 含点光源/线性深度处理)
    - PS#2835 使用 CCShadow CB (CB0[14]), 包含 matLightView/NFLSInfo/LPNNInfo
    - 支持点光源阴影 (sphere light): `length(lightViewPos) - near / (far - near)`
    - 支持线性深度编码 (VSM): frac(v_clip_depth * [1,255,65025,16581375]) + decode
- **Pass #2 PS#2840**: 完整 PBR + shadow sampling
  - Shadow 采样: `step(shadowPos.z, cc_shadowMap.Sample(sampler, shadowPos.xy).x)` (line 123-127)
  - UV 变换: `shadowPos.xy / shadowPos.w * 0.5 + 0.5` (NDC→UV)
  - 支持 Y 翻转: `cc_cameraPos.w == 1.0` 时 `UV.y = 1.0 - UV.y` (line 106-113)
  - Shadow 值 `_1548` 混入光照: `directLighting *= _1548` (line 184)

## Shadow 调试综合分析（本次会话续）
- **完整代码审查**: 审查了 PipelineLayout、DescriptorSet、CommandBuffer 的所有阴影相关代码路径
- **7个假设全部排除**: H1(Y-flip)、H2(深度编码)、H3(空纹理)、H4(Viewport)、H5(资源状态)、H6(CB映射)、H7(描述符时序)
- **Shader 数学验证**: Pass #1 输出 z/w [0,1]，Pass #2 采样 + step 比较，数学完全正确
- **Root Signature 映射验证**: Shader 编译管线的 register_space/register_binding 与 PipelineLayout 的 BaseShaderRegister/RegisterSpace 完全一致
- **Y-flip 验证**: D3D12 combineSignY=1（与 Metal 相同），shader 中 `cc_cameraPos.w==1.0` 正确触发翻转
- **shadowProjDepthInfo 验证**: `(m10, m14, m11, m15)` = `(M_22, M_32, M_23, M_33)` = 正交投影深度参数，正确
- **报告**: AI/d3d12-shadow-analysis.md
- **关键限制**: RenderDoc MCP 不支持 GetConstantBuffer，无法直接验证 CB 数据值
- **下一步**: 需要验证 CBV 实际数据内容（UBO 上传路径、对齐、cc_mainLitDir.w 阴影开关）

## Shadow 调试第三轮 — UBO 上传路径验证
- **UBO 上传路径验证完成**: Buffer 创建 256B 对齐 ✅, updateBuffer Map/memcpy/Unmap ✅, CBV SizeInBytes ≥ 256 ✅
- **UBOShadow SIZE**: 256B (64+64+16*8 = 10个 vec4 × 4 = 40 floats × 4B = 160B... 不对，COUNT=64, SIZE=256B)
- **flushDescriptorSets 验证**: CopyDescriptorsSimple 从 CPU heap 起始复制所有 CBV/SRV/UAV，与 Root Signature Range 顺序一致 ✅
- **添加运行时诊断日志**: D3D12CommandBuffer::updateBuffer 中添加了 Shadow UBO 和 Camera UBO 关键字段打印
  - Shadow UBO: matLightViewProj[4行], shadowProjDepthInfo, shadowNFLSInfo, shadowWHPBInfo
  - Camera UBO: cc_mainLitDir.xyz + shadowEnable(.w), cc_cameraPos.xyz + combineSignY(.w)
  - 每种 UBO 限制输出前 5 次
- **编译成功** (Debug 配置)
- **PS 反汇编确认**: 阴影分支在 capture 中确实执行了（从 draw call 结构可推断），cc_mainLitDir.w > 0

## Shadow 调试第四轮 — 诊断日志分析
- **用户运行测试**，输出关键日志:
  - `[D3D12-SHADOW-UBO]` size=256: matLightViewProj 和 shadowProjDepthInfo 数据正确 ✅
  - `[D3D12-CAMERA-UBO]` size=656: cc_mainLitDir=(0,0,0) shadowEnable=0 cc_cameraPos=(0,0,0) combineSignY=0 ❌ **全零！**
- **关键发现**: Camera UBO 存在两条上传路径:
  1. **Buffer::update()** — `updateMultiCameraUBO` (ForwardPipeline 主路径，line 555): 直接 Map/Unmap，不经过 CommandBuffer
  2. **CommandBuffer::updateBuffer()** — `updateCameraUBO` (ReflectionProbeStage 调用，line 526): 经 CommandBuffer
- **问题**: 诊断日志只截获了路径 2（ReflectionProbe 的 probe camera 数据为全零），**未截获主路径 1**
- **size=656 分析**: 非 UBOCamera::SIZE(592) 亦非 aligned(768)，可能是其他大 buffer 的 updateBuffer 调用
- **新增诊断**: 
  - `D3D12Buffer::update()` 中添加 `[D3D2-BUF-CAMERA-UBO]` 日志，截获 Buffer::update() 路径
  - 增强 `D3D12CommandBuffer::updateBuffer()` 日志，打印 bufSize/resOffset/gpuAddr 帮助识别 buffer
- **编译成功**，等待用户测试
- **下一个验证点**: Buffer::update() 路径的 Camera UBO 数据是否正确
