# 2026-05-23

## D3D12 Shadow 不显示 — 调查进展（第三轮）

### 前两轮已排除
1. **第一轮**：`PipelineUBO::updateMultiCameraUBO` resize 后漏绑 pipeline descriptor set — 修复后仍无阴影。Camera UBO 数据实际正确。
2. **第二轮**：`ShadowFlow::activate()` 中 `CC_SHADOW_TYPE` 硬编码为 0 — 用户反馈此修改不影响（Vulkan/GLES 也用同样的宏但 shadow 正常），已回退。

### 第三轮发现
1. **Shader 编译日志**确认所有 shader 用 `CC_SHADOW_TYPE0`，但用户说 Vulkan/GLES 正常
2. Shadow map 是 R32F **color attachment**（不是 depth texture），编码深度到 color
3. Camera UBO 数据确认正确（`first4=(1.0,0.0,0.0,0.0)` = cc_matView[0]）
4. Shadow UBO 数据确认正确（matLightViewProj 有值）
5. SPIRV-Cross HLSL 中无 UBOShadow/cc_shadowMap（因 `CC_SHADOW_TYPE=0` DCE）

### 已保留的修改
- `D3D12CommandBuffer::endRenderPass`：depth stencil 从 DEPTH_WRITE 转为 `DEPTH_READ | PIXEL_SHADER_RESOURCE`（允许 depth texture 作 SRV 读取，虽然不影响 shadow map）

### 诊断代码已清理
- D3D12DescriptorSet.cpp 中所有诊断日志已移除

### 待确认
- 用户需要用 RenderDoc 抓 D3D12 capture，对比 Vulkan 的实际渲染差异
- 特别关注 shadow pass 是否有 draw call、shadow map texture 是否有内容
- 确认 Vulkan 后端 shader 编译时是否确实用了 `CC_SHADOW_TYPE0`

## RenderDoc Capture 分析（2026-05-23 20:53）

### 分析任务
- 分析 RenderDoc capture 文件：`C:/Users/caosh/Desktop/gles3-shadow.rdc`
- API: OpenGL (GLES3)
- 输出报告至：`D:\cocos_custome\cocos-engine\AI\analysics\gles3-shadow-analysis.md`

### 关键发现
1. **渲染架构**：6 个主要渲染 pass
   - Pass #1: 阴影贴图渲染 (2048x2048, R32_FLOAT + D32)
   - Pass #2: 主场景渲染 (800x600, RGBA8 + D32) - 16 个 draw calls
   - Pass #3: 中间渲染 (6 索引小绘制)
   - Pass #4: 最终合成

2. **纹理资源**：
   - 阴影贴图：2048x2048 R32_FLOAT (16MB) + D32 (16MB)
   - 主渲染目标：800x600 RGBA8 (1.92MB)

3. **Shader 绑定**：
   - Vertex Shader: CCLocal (224B), CCShadow (256B), Constants (96B)
   - Pixel Shader: CCCSM (656B), CCCamera (592B), CCGlobal (80B), CCShadow (256B)

4. **绘制调用统计**：29 个 draw calls
   - 600 索引：10 次（主要几何体）
   - 6 索引：11 次（简单几何体/全屏四边形）
   - 54 索引：7 次（中等复杂度）

### 优化建议
1. 批处理 11 个 6 索引的小绘制调用（使用实例化渲染）
2. 考虑合并 Pass #3 和 Pass #4 减少状态切换
3. 阴影贴图分辨率可能需要根据场景调整

### 工具使用
- 使用 RenderDoc MCP 工具：`get_capture_status`, `get_frame_summary`, `get_draw_calls`, `get_draw_call_details`, `get_texture_info`, `get_pipeline_state`
